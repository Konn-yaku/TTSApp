# TTSApp 2.0

上一版本：v1.0.1

## 新增

- **界面日志区**：显示每一条语音的合成与播放情况。每条输入带 `#N` 编号，并给出「合成 / 排队 / 解码」的耗时分解，可选中复制。
- **连接预热**：启动时，以及长时间停顿后再次输入时，自动提前建立与服务器的连接，让首次发音（以及停顿后的第一句）更快出声。
- **超时自动重发**：网络抖动造成超时时自动重发一次，不需要再手动按一次回车。

## 改进

- **严格按输入顺序播放**：连续快速输入多句时，不再出现短句抢先播出。
- **请求不会无限等待**：加了总时长上限，一句话最多等几秒就会有结果或明确提示。
- **首次合成更快**：复用已有连接，省掉每次重新握手的时间。
- **含 `<`、`&` 等符号的文本也能正常发音**。
- **从任意目录启动都能正常找到 config 与 cache**。

## 配置

`sound_model.json` 新增两项（一般无需修改）：

| 项目 | 含义 | 默认值 |
| --- | --- | --- |
| `WARM_CONNECTIONS` | 同时合成的句数上限 | 2 |
| `IDLE_REWARM_SECONDS` | 停顿多久后重新连接服务器 | 100 |

## 注意

- 请保持 `app.exe`、`config/`、`cache/` 的相对位置不变。
